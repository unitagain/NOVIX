# Desktop Icon Assets

Place platform icons here before producing signed public builds.

Expected files:

- `wenshape.ico` for Windows
- `wenshape.icns` for macOS

The phase 4 packaging pipeline treats these files as optional so the build can still run in engineering environments without final brand assets.
